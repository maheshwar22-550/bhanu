// JavaScript functions for button clicks

// Function to handle navigation
function navigateTo(page) {
    alert('Navigating to ' + page + ' page.');
    // In a real application, this could redirect to a new URL like:
    // window.location.href = page + ".html";
}

// Function to perform action on the buttons in the content section
function performAction(action) {
    alert('Performing ' + action + ' action.');
    // Here, you can replace the alert with real functionality.
    // Example: Redirect to a page for the action or open a modal
}
